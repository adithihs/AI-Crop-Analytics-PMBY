# Original Team Model Artifacts

These files are retained from the original team application package:

- `crop_model.h5`
- `crop_model_v2.keras`

The portfolio version of the application uses the hybrid PyTorch damage/disease model in `models/` as the primary inference model. The original Keras artifacts are retained here so the full application package and earlier team model files remain documented together.
